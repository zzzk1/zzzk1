name: Waka Readme
on:
  schedule:
    - cron: "*/10 * * * *"
  # Manual triggers with workflow_dispatch
  workflow_dispatch:
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Update gist
        uses: zzzk1/waka-box@master
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: 5cab4fa78d36f86361db7ae9a38a7f05
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
